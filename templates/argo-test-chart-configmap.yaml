apiVersion: v1
kind: ConfigMap
metadata:
  namespace: argo-test-chart
  name: argo-test-chart-configmap
data:
  index.html: |
    <html>
    <body>
    <h1>{{ .Values.greeting }}</h1>
    </body>
    </html>
